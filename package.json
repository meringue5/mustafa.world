{
  "name": "mustafa-world",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build:world": "node scripts/build-world.mjs",
    "predev": "npm run build:world",
    "dev": "vite --host 127.0.0.1",
    "prebuild": "npm run build:world",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "vite": "^7.0.0",
    "yaml": "^2.9.0"
  }
}
