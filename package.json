{
  "name": "mustafa-world",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build:world": "node scripts/build-world.mjs",
    "build": "npm run build:world && node scripts/build-site.mjs"
  },
  "dependencies": {
    "@xterm/xterm": "^5.5.0",
    "yaml": "^2.9.0"
  }
}
